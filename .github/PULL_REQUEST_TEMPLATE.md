## Outcome

## Scope

## Risk boundary

`FAST` / `SAFE` / `CRITICAL` and the concrete changed boundary.

## Changed surfaces / ownership

## Checks

Use `LOCAL`, `CI`, `STAGING`, `PROVIDER` and `PRODUCTION` only for evidence actually obtained.

## Dependencies / integration order

## Rollback or recovery

## Live actions

None unless separately authorized. State any merge, deploy, publication, migration, activation,
provider write or live/shared mutation explicitly.
